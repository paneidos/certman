[tool.poetry]
name = "certman"
version = "0.1.0"
description = ""
authors = ["Sernin van de Krol <serninpc@paneidos.net>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
typer = {extras = ["all"], version = "^0.9.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
